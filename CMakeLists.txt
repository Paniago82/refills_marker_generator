cmake_minimum_required(VERSION 2.8.3)
project(refills_marker_generator)

find_package(catkin REQUIRED COMPONENTS
  rospy
)

catkin_package(
#  INCLUDE_DIRS include
#  LIBRARIES refills_marker_generator
#  CATKIN_DEPENDS rospy
#  DEPENDS system_lib
)
